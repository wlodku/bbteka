<%= form_for(book) do |f| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
      <%= f.label :isbn, "ISBN" %><br/>
      <%= f.text_field :isbn, autofocus: true, class: 'form-control' %>
  </div>
  <div class="form-group">
      <%= f.label :title, "Tytuł" %><br/>
      <%= f.text_field :title, class: 'form-control' %>
  </div>
  <%= f.fields_for :authors do |a| %>
    <div class="form-group">
        <%= a.label :title, "Autor (autorzy)" %><br/>
        <%= a.text_field :name, class: 'form-control' %>
        <%= a.text_field :surname, class: 'form-control' %>
    </div>
    <% end %>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
